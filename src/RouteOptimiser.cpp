#include <bits/stdc++.h>

using namespace std;

class routeOptimise {
public:
    double OptimalPath(int nodes, vector<vector<pair<int, int>>> geoHash, map<int, int> orderMap) {
        return 0.0;
    }
};