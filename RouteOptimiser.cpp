#include <bits/stdc++.h>

using namespace std;

class routeOptimise {
public:
    double optimalTimeRequired(vector<vector<pair<int, int>>> graph, map<int, int> orderMap) {
        double time;
        return time;
    }
};